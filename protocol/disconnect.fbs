// PrivChat protocol — disconnect handshake.

include "common.fbs";

namespace privchat.protocol;

table DisconnectRequest {
  reason  : DisconnectReason;
  message : string;             // optional
}

table DisconnectResponse {
  acknowledged : bool;
}

root_type DisconnectRequest;
